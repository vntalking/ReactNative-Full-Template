import * as ActionTypes from './actionTypes';

const initState = {
  logged: false
}
export function AuthReducer(state = initState, action) {
  switch (action.type) {
    default:
      return state;
  }
}